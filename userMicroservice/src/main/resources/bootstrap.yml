spring:
  application:
    name: user-api
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  config:
    import:
      - optional:configserver:http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, info