spring:
  application:
    name: user-api
  cloud:
    config:
      uri: http://localhost:8888
      name: user-api
      profile: default
  config:
    import:
      - optional:configserver:http://localhost:8888